express
------------
npm package
frame work
manages everything from receving the request and givving the response

//routes
facebook.com/
youtube.com/profile
app.get(route, requestHandler);
->request handler is an middleware.

//middleware
client-->(req)-->middleware1(do something)-->middleware2(do something)-->routes-->middleware(error handler)
app.use(function(req,res,next){});


//error handler
app.use((err, req, res, next) => {
    console.error(err.stack)
    res.status(500).send("Something Went Wrong, We don't have any idea");
})

//cookie
use in login and log out